#include <stdio.h>
int main(void)
{
    // unsigned i;
    // for ( i = cnt -2; i >=0 ; i--)
    // {
    //     a[i] += a[i+1];
    // }

    //proper way use unsigned as loop index;

    // unsigned i
    // for (i =cnt -2;i<cnt;i--){
    //     a[i] += a[i+1];
    //     //i-->Umax退出
    // }
}

// 在 32 位架构中被普遍定义为：

// typedef   unsigned int size_t;

// 而在 64 位架构中被定义为：

// typedef  unsigned long size_t;
